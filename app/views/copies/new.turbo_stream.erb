<%= turbo_stream.update "#{dom_id(@copy)}_characters" do %>
  <%= render "characters", copy: @copy %>
<% end %>

<%= turbo_stream.update "#{dom_id(@copy)}_characters_print" do %>
  <%= render "characters", copy: @copy %>
<% end %>
